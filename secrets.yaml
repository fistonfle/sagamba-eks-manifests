# Template: replace values and apply. Prefer external secrets (AWS Secrets Manager / EKS External Secrets) in production.
apiVersion: v1
kind: Secret
metadata:
  name: sagamba-secrets
  namespace: sagamba
  labels:
    app.kubernetes.io/part-of: sagamba
type: Opaque
stringData:
  POSTGRES_USER: "sagamba_user"
  POSTGRES_PASSWORD: "CHANGE_IN_PRODUCTION"
  DB_USERNAME: "sagamba_user"
  DB_PASSWORD: "CHANGE_IN_PRODUCTION"
  REDIS_PASSWORD: "CHANGE_IN_PRODUCTION"
  RABBITMQ_USERNAME: "sagamba_user"
  RABBITMQ_PASSWORD: "CHANGE_IN_PRODUCTION"
  JWT_SECRET: "CHANGE_IN_PRODUCTION_USE_AT_LEAST_32_CHARS"
